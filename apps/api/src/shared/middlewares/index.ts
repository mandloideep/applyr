// Request logging with correlation IDs
export { requestLogger } from "./requestLogger";

// Global error handler
export { errorHandler, handleUncaughtException, handleUnhandledRejection } from "./errorHandler";

// Phase 5: rate-limit, validate
// Phase 6: auth guards
